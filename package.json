{
  "name": "weatherapi",
  "version": "1.0.0",
  "description": "An api to get the current weather and forecasts for a given location",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "tsc && node ./dist/",
    "watch": "tsc && nodemon ./dist/"
  },
  "author": "vVanish",
  "license": "MIT",
  "dependencies": {
    "@cicciosgamino/openweather-apis": "^5.2.0",
    "openweather-apis": "^4.4.2",
    "weather-js": "^2.0.0"
  }
}
