{
  "name": "weatherapi",
  "version": "1.0.0",
  "description": "An api to get the current weather and forecasts for a given location",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "tsc && node ./dist/"
  },
  "author": "vVanish",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "typescript": "^4.6.3",
    "weather-js": "^2.0.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/dotenv": "^8.2.0"
  }
}
